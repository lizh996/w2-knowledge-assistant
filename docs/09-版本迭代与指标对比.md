# M5 版本迭代文档 + 优化指标对比

> **文档来源**：真实迭代过程的记录（git 提交历史 + 每次评测结果）——不是事后编的
> **阶段**：M5（V2→V3 迭代完成后）
> **日期**：2026-08-29

---

## 一、版本总览（git 提交对应）

| 版本 | git 提交 | 定位 | 核心内容 |
|---|---|---|---|
| V1 | 591dd25 | MVP 基线 | 数据管线跑通 + 问答带页码溯源 |
| M3 评测 | b066501 | 基线建立 | 评测集 12+6+10 + 基线 recall 0.9167 / MRR 0.6625 |
| V2/V3 文档 | 32c3519 | 增强+进阶 | trace + 复杂元素 + 复合检索 + 精排 + 改写 |
| 修复 | c5ab598 | 多轮修复 | 追问拒答误判修复 |

## 二、V1 迭代记录（MVP）

**做了什么**：3 份国标 → fitz 解析 → 结构分块（≤512 token）→ BGE-M3 向量化 → Qdrant 入库 → FastAPI /ask → 前端问答。

**遇到的问题**：
1. **并发向量化 meta tensor 崩溃**：3 个上传任务同时加载模型 → 多实例冲突。
   **解决**：vectorize_chunks 复用 retrieve 模型单例 + 加锁（_VECTORIZE_LOCK）。
2. **Qdrant 单进程锁**：服务运行时评测脚本连不上。
   **解决**：评测前停服务，跑完再启动。

**验证**：检索集 12 题 recall@5=0.9167、MRR=0.6625；拒答 10 题 100%。

## 三、V2 迭代记录（增强）

**做了什么**：
1. **trace 可观测**：/ask 返回 8-10 步链路（每步耗时），检索测试台四路分数。
2. **复杂元素提取**：表=整表原子块；图=图语义块；公式=LaTeX 保留。
3. **复合检索**：dense+sparse 双向量 → Qdrant FusionQuery(RRF, k=60)。

**为什么**：任务书 V2 定义"引入 trace + 复杂元素提取 + 复合检索"。trace 是后续优化的地基——没有它，V3 精排/改写的效果无法归因。

**遇到的问题**：检索测试台 rerank 通道是假的（只复制 RRF 分没跑模型）。**解决**：补真 reranker 调用。

## 四、V3 迭代记录（进阶）

**做了什么**：
1. **reranker 精排**：粗排 15 条 → bge-reranker cross-encoder → 取 5 条。
2. **查询改写**：LLM 口语 query → 规范术语，QUERY_REWRITE_ENABLED 开关+缓存+兜底。
3. **上下文组织**：score 过滤 + embedding 去重 + token 预算 + MIN_KEEP=2。
4. **citations 诚实化**：引用只列实际进 LLM 的块。

**验证**：MRR 0.6625→0.6694（#4 题"重复性要求"从漏检→rank2 救回）。召回持平。

---

# 优化指标对比文档

## 版本对比表

| 版本 | recall@5 | MRR | 拒答准确率 | 说明 |
|---|---|---|---|---|
| V1 基线 | 0.9167 | 0.6625 | 100% | 朴素检索 |
| V2 增强 | 0.9167 | 0.6625 | 100% | +trace/复杂元素/复合检索（可观测性提升）|
| V3 进阶 | 0.9167 | **0.6694** | 100% | +精排/改写/上下文组织 |

**MRR 提升归因**：
- **精排**：粗排 15 条时真答案可能排 rank7+，cross-encoder 精排救回前 5
- **查询改写**：口语问题改写为术语后与 chunk 的 question_kwd 对齐
- **上下文组织**：过滤负分噪音块，证据更干净

**仍弱项**：公式类题目检索命中率低——复杂元素（公式）语义化编码待优化（列入后续版本）。

---

# 决策记录（D1-D6）

| # | 决策 | 阶段 | 为什么 |
|---|---|---|---|
| D1 | 数据选 3 份变压器国标 | M1 | 同一业务域，多文档检索有真实场景 |
| D2 | 技术栈复用 W2 工具链 | M2 | 任务书允许；已验证能力×成本最优 |
| D3 | V2 先做 trace | M5 | 没有 trace 无法归因后续优化 |
| D4 | 精排用开关（RERANK_ENABLED）| M5 | 单文档负收益、多文档正收益——开关让评测决定 |
| D5 | 查询改写开关+兜底 | M5 | 改写引入噪音风险，开关可回退 |
| D6 | citations 只列实际用块 | M5 | 诚实原则：引用=答案真正依据 |

---

# 附：评测环境与口径说明（读数字前必看）

> 本文档记录 M5 阶段的迭代数据（2026-08-29）；W3 微调阶段（2026-09-08）的评测口径不同，**跨表数字不可直接比较**。

| 数字 | 题集 | 模型 | 库 | 日期 | 用途 |
|---|---|---|---|---|---|
| recall@5 **0.9167** / MRR 0.6625→**0.6694** | 12 题版本集 | base（未微调）| 88 块 | 08-29 | 迭代期**快速回归**（守门）|
| recall@5 **0.880** / **0.9371** | 350 题原始集（含 86 条 train-query 重叠）| base / FT | 88 块 | 09-06 | 初版口径，**已废弃**（含背题水分）|
| recall@5 **0.890** / **0.9280** | **264 题干净集** | base / **E1 微调** | 88 块 | 09-08 | **W3 正式基线 / 最终成绩** |
| recall@5 **0.9167** | 12 题版本集 | **E1 微调** | 149 块 | 09-08 | 上线后**回归验证**（守住不降）|

**为什么数字不一样**：三处口径都不同——① 题集规模（12 题快速回归集 vs 264 题正式干净集）；② 模型（base vs `bge-m3-ft-v1`）；③ 库版本（88 文本块 vs 149 块含图语义块）。**同一行内前后可比，跨行不可直接比较。**

**关键修正记录（数据真实性）**：350 题原始集里有 86 条（约 25%）与训练问法重叠（`source=train-query`，属背题），已从评测源剔除 → 得到 264 题干净集；微调真实提升由 **+5.7pp 修正为 +3.8pp**，MRR 由 +5.6pp 修正为 +4.1pp。

**W3 微调完整实验**（E0/E1/E2、Dense/Sparse/Fusion 三通道分组、Saved/Degraded 四象限、Unseen Knowledge 46131、OOD 20 条、RAGAs 答案层 4 指标）见 `docs/11-微调实验记录.md`；对外报告见 `W3嵌入模型微调-评测报告`。
