# 微调实验记录（嵌入模型 BGE-M3 LoRA）

> **阶段**：W3（嵌入模型微调）
> **日期**：2026-09-01（E0/E1）、2026-09-08（泄漏修正 + E2 计划）
> **用途**：微调实验的正式指标记录，E1/E2 对比的唯一权威口径

---

## 一、实验定义

| 实验 | 模型 | 说明 |
|---|---|---|
| E0 | base BGE-M3 | 基线，未微调 |
| E1 | base + LoRA (`output_ft_v1`) | 800 条训练数据，InfoNCE 对比学习（对角线已屏蔽） |
| E2 | base + LoRA (`output_ft_v2`) | hard negative + 去重 + 干净评测（已跑，见 §四） |

**训练**：800 条 `{"query","pos","neg"}`，LoRA(r=8, query/value)，lr=1e-4，3 epoch，等效 batch 8。
**评测**：CLS 池化 + L2 归一化 + 余弦，88 块 chunk 全库暴力检索，Recall@5 / MRR@10 / Top1。

---

## 二、评测集泄漏问题（已修正）

初版评测集 `eval_retrieval_set.json` 共 350 条，其中 **86 条 `source=train-query`** 的 question 与 `train.jsonl` 的 query **100% 完全匹配**——由 `gen_data.py`「用训练 query 补评测集」逻辑引入，属于**拿训练集当评测集**的泄漏。

| 口径 | 评测题数 | base recall@5 | ft recall@5 | ft MRR@10 |
|---|---|---|---|---|
| 含泄漏（350 条） | 350 | 0.8800 | 0.9371 | 0.7473 |
| **干净（剔除 86 条）** | **264** | **0.8902** | **0.9280** | **0.7310** |

泄漏使 ft 的 recall 提升虚高（+5.7pp → 真实 +3.8pp），MRR 提升同样缩水。**已根治**：`gen_data.py` 删除补评测集逻辑，评测集仅保留 `question_kwd` 种子，与训练 query 零重叠。

---

## 三、干净基线结果（权威口径）

`eval_clean.py` 剔除 86 条泄漏样本后，264 题干净集实测：

| 指标 | E0 (base) | E1 (ft) | 提升 |
|---|---|---|---|
| Recall@5 | 0.8902 | 0.9280 | **+3.8pp** |
| MRR@10 | 0.6900 | 0.7310 | **+4.1pp** |
| Top1 | 0.5455 | 0.5909 | +4.5pp |

**结论**：E1 微调在干净口径下仍有正向收益（Recall@5 89%→93%，MRR 69%→73%），收益真实、非背题所得。

---

## 四、E2 重训计划（进行中）

E1 的已知缺陷与 E2 修复项：

| # | 缺陷 | E2 修复 |
|---|---|---|
| 1 | 训练数据含 12 条歧义 query（同 query 对应多个不同 gold） | 删除歧义样本（792→768 条） |
| 2 | hard negative 是「同文档随机抽 2 块」，非真难样本 | base 模型检索 top2 非 gold 块作 hard neg |
| 3 | pos/neg 长度不对称（600 vs 300，可学长度捷径） | 统一截断 512 |
| 4 | InfoNCE in-batch 负例用 q×query | 改用 q×其他样本 positive（方向性修正） |
| 5 | 梯度累积首步即 step / T_max 错配 | 修正计数 + T_max 对齐 |
| 6 | 无验证集、无早停 | 按 chunk 划分验证集 + 早停 |

E2 产出：`train_clean.jsonl`（768 条）+ `output_ft_v2/`。

**E2 实测结果（264 题干净集，2026-09-08）**：

| 指标 | E1 (ft_v1) | E2 (ft_v2) | 变化 |
|---|---|---|---|
| Recall@5 | 0.9280 | 0.9242 | -0.38pp |
| MRR@10 | 0.7310 | 0.7270 | -0.40pp |
| Top1 | 0.5909 | 0.5795 | -1.14pp |

**结论**：E2 略逊于 E1（-0.4pp 左右），**保留 E1，不部署 E2**。E2 的硬负例 + 去重 + in-batch 修正未带来增益，可能因训练数据量小（768 条）或硬负例与 gold 语义仍偏近（同文档相邻块），指标波动在噪声范围内。E2 脚本改进保留供后续数据量增大后重试。

---

## 五、补齐评测（讲师 V2.0 §8.3/§8.4，2026-09-09）

在 264 干净集基线之外，补齐 3 项评测，覆盖「检索通道分组 / 未见知识泛化 / OOD 鲁棒性」。

### 5.1 Dense / Sparse / Fusion 分组评测（§8.3）

用 FlagEmbedding 1.4.2 的 `BGEM3FlagModel` 加载 base 与 ft(E1)，对 264 干净集、88 块候选库（`chunks_all.json`）分别跑三种检索通道的 recall@10。

| 通道 | base | ft(E1) | 变化 |
|---|---|---|---|
| dense-only | 0.9583 | 0.9697 | **+1.1pp** |
| sparse-only | 0.9811 | 0.9848 | +0.4pp |
| RRF-fusion (k=60) | 0.9773 | 0.9773 | 0.0pp |

**口径**：dense = CLS 池化 + L2 归一化余弦；sparse = `lexical_weights` 查询侧 IDF 加权（`idf = ln((n-df+0.5)/(df+0.5)+1)`，对齐 Qdrant local mode）；fusion = RRF(k=60，对齐线上 `retrieve.py _RRF_K`）。

**结论**：**Dense ↑**（0.958→0.970，微调主收益通道）；**Sparse 基本持平**（+0.4pp，LoRA 只训 query/value 注意力、sparse_linear 共享主干未训，符合「共享主干」预期）；**Fusion 持平**（base 的 sparse 本已很强，fusion 上限由 dense 短板决定，ft 的 dense 提升被 sparse 高位抵消，recall@10 已近饱和）。三者与预期方向一致（Dense ↑、Sparse 持平/微降、Fusion ↑ 或持平）。

### 5.2 Unseen Knowledge 子集（46131，泛化验证）

素材：GB/T 46131（特高压变压器分接开关）61 块，**未参与训练**，从 qdrant 上传集合 `transformer_upload_6d10895f0f2448c4ab374454b57631d1` 导出（`chunks_46131.json`）。DeepSeek 生成 28 条问题（含 gold_chunk_id，覆盖 5 类 semantic_type）。候选库 = 88 + 61 = 149 块，gold 全为 46131 块。

| 指标 | base | ft(E1) | 变化 |
|---|---|---|---|
| Recall@5 | 0.9643 | 0.9286 | -3.6pp（差 1 题/28） |
| MRR@10 | 0.8304 | 0.8245 | -0.6pp |

**结论**：**FT 未破坏泛化**。28 题小样本下单条即 3.6pp，recall 差 1 题、MRR 仅降 0.6pp，属单样本噪声；未见知识上 ft 仍达 92.9% 召回，微调未「背题式」过拟合到 88 块训练域。

### 5.3 OOD 检索子集（鲁棒性）

从 264 干净集抽 20 条，人工加错别字/口语化/新术语变体（gold 不变），类型分布：colloquial 15 / newterm 3 / typo 2。

| 指标 | base | ft(E1) | 变化 |
|---|---|---|---|
| Recall@5 | 0.9000 | 0.9500 | **+5.0pp** |

**结论**：**FT 在 OOD 上不明显退化，反而略优**（18/20→19/20）。口语化/错别字/新术语改写下 ft 的语义匹配更稳，未见鲁棒性损失。

---

## 六、相关产物

| 产物 | 路径 | 说明 |
|---|---|---|
| 训练脚本 | `finetune/train_lora_bgem3.py` | E2 修复版 |
| 数据生成 | `finetune/gen_data.py` | 已删泄漏逻辑 |
| hard neg 生成 | `finetune/gen_hard_neg.py` | base 检索 hard neg + 去重 |
| 干净评测 | `finetune/eval_clean.py` | 剔除 train-query 后评测 |
| E1 模型 | `finetune/output_ft_v1/` | 当前部署 `data/models/bge-m3-ft-v1` |
| 共享评测工具 | `finetune/_eval_common.py` | 加载 base/ft + dense/sparse/IDF/RRF 打分 |
| 分组评测 | `finetune/eval_channels.py` | Dense/Sparse/Fusion recall@10 |
| 未见导出 | `finetune/export_chunks_46131.py` | 导出 46131 的 61 块 → `chunks_46131.json` |
| 未见问题生成 | `finetune/gen_unseen_46131.py` | DeepSeek 生成 28 条 → `eval_unseen_46131.json` |
| 未见评测 | `finetune/eval_unseen.py` | 149 块候选，recall@5/MRR |
| OOD 评测 | `finetune/eval_ood.py` | 20 条变体，recall@5 |
